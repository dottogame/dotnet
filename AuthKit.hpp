#pragma once

#include <SFML\Network.hpp>

#include "TwoStateDotocol.hpp"

namespace authkit {
    void check(char* data, sf::IpAddress& sender, unsigned short& port)
    {
        // TODO check
        // TODO build connection object if correct
    }
}